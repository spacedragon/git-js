{
  "name": "@elastic/simple-git",
  "description": "Simple GIT interface for node.js, bundled with native git natived",
  "version": "1.124.0-elastic.2",
  "author": "Steve King <steve@mydev.co>",
  "contributors": [
    {
      "name": "Yulong Huang",
      "email": "yulong.huang@elastic.co"
    }
  ],
  "dependencies": {
    "debug": "^4.0.1",
    "request": "^2.88.0",
    "targz": "^1.0.1"
  },
  "devDependencies": {
    "@kwsites/test-runner": "^0.1.1",
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.3",
    "aws-sdk": "^2.503.0",
    "glob": "^7.1.4",
    "jest": "^24.9.0",
    "sinon": "^7.3.2",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.3.0",
    "typescript": "^3.6.2"
  },
  "keywords": [
    "git",
    "source control",
    "vcs"
  ],
  "license": "MIT",
  "repository": "https://github.com/elastic/simple-git.git",
  "main": "./dist/index.js",
  "files": [
    "dist/",
    "typings/",
    "scripts/"
  ],
  "scripts": {
    "build": "tsc -p .",
    "types": "tsc -d --emitDeclarationOnly --allowJs false --declarationDir ./typings",
    "test": "yarn run build && jest && yarn run test:unit && yarn run test:integration",
    "test:unit": "node test/runner.js test/unit/test*.*",
    "test:integration": "node test/runner.js test/integration/test*.js",
    "preversion": "yarn test",
    "postversion": "npm publish && git push && git push --tags",
    "postinstall": "node scripts/postinstall.js",
    "prepublish": "yarn run build && yarn run types"
  },
  "git_natives": {
    "linux": {
      "version": "2.22.0",
      "build": "1"
    },
    "darwin": {
      "version": "2.21.0",
      "build": "954b7fe"
    },
    "win32": {
      "version": "2.21.0",
      "build": "954b7fe"
    }
  }
}
